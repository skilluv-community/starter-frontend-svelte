# Vite exposes only variables prefixed with PUBLIC_ to the client.
PUBLIC_APP_NAME="Skilluv starter — Svelte"

# Optional: base URL of an external API consumed by /api-demo.
PUBLIC_DEMO_API_BASE="https://api.github.com"

# Dev server port.
PORT=5173
